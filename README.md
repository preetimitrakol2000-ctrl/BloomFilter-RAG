# BloomFilter-RAG // Probabilistic Document Gating Layer

A probabilistic preprocessing barrier implementing low-overhead string hashing algorithms in C bit-arrays to intercept and short-circuit non-existent vector queries.

## Execution
```bash
python gate_rag_pipeline.py
